<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Book a Tour | THAI TOUR</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="icon" type="image/png" href="/uploads/Logo-company.png">

  <style>
    .form-control {
      border-radius: 12px;
    }

    .btn-submit {
      background-color: #005cbf;
      color: white;
      border-radius: 30px;
      padding: 10px 30px;
      font-weight: bold;
      border: none;
    }

    .btn-submit:hover {
      background-color: #004999;
    }
  </style>
</head>
<body class="bg-light">

  <!-- Navbar -->
  <%- include('../partials/navbar-user', { active: '' }) %>

  <div class="container d-flex justify-content-center align-items-center" style="min-height: 85vh;">
    <div class="card shadow p-4 w-100" style="max-width: 600px;">
      <h4 class="fw-bold mb-4 text-center">BOOKING</h4>

      <form method="POST" action="/user/book">
        <!-- Tourist Spot -->
        <div class="mb-3">
          <label class="form-label">Tourist Spot</label>
          <input type="text" class="form-control" name="spot_name" value="<%= spot.spot_name %>" disabled>
          <input type="hidden" name="spot_name" value="<%= spot.spot_name %>">
        </div>

        <!-- Guide -->
        <div class="mb-3">
          <label class="form-label">Guide</label>
          <select class="form-control" name="guide_id" required>
            <option value="">-- Select Guide --</option>
            <% guides.forEach(guide => { %>
              <option value="<%= guide.guide_id %>"><%= guide.full_name %> (<%= guide.language_spoken %>)</option>
            <% }) %>
          </select>
        </div>

        <!-- Date -->
        <div class="mb-3">
          <label class="form-label">Date</label>
          <input type="date" class="form-control" name="booking_date" required>
        </div>

        <!-- People -->
        <div class="mb-3">
          <label class="form-label">Number of People</label>
          <input type="number" class="form-control" name="number_of_people" min="1" required>
        </div>

        <!-- Submit -->
        <div class="text-center">
          <button type="submit" class="btn btn-submit">SUBMIT</button>
        </div>
      </form>

      <a href="/user/spots" class="btn btn-secondary mt-4 w-100">Back</a>
    </div>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>
